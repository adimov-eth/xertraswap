var _a;
import { variants } from "./types";
export var styleVariants = (_a = {},
    _a[variants.ROUND] = {
        borderRadius: "32px",
    },
    _a[variants.FLAT] = {
        borderRadius: 0,
    },
    _a);
export default styleVariants;
